# address_standardizer extension
comment = 'Parse addresses into constituent elements (street name, number, etc)'
default_version = '3.7'
relocatable = true
module_pathname = '$libdir/address_standardizer'
